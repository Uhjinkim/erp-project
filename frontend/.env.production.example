# Copy to frontend/.env.production before the production build.
VITE_APP_ENV=production
VITE_AUTH_MODE=session
# Leave empty for same-origin /api requests, or provide the public backend origin.
VITE_API_BASE_URL=
VITE_API_TIMEOUT_MS=5000
VITE_HEALTH_POLL_INTERVAL_MS=30000
